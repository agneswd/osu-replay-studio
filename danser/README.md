# Danser

`npm run setup` installs Danser 0.11.0 in the ignored `runtime/danser/` folder.
Keep its executable, libraries, and `assets.dpak` together.
Each render uses a temporary settings folder and database.
